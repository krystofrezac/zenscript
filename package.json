{
  "name": "boring-lang",
  "version": "0.0.0",
  "author": "Kryštof Řezáč",
  "scripts": {
    "dev": "npm run gen:bundle && tsc --watch",
    "start": "node dist/index.js",
    "gen:bundle": "ohm generateBundles --withTypes src/*.ohm && cp src/grammar.ohm-bundle.* dist/"
  },
  "dependencies": {
    "ohm-js": "^16.6.0",
    "typescript": "^4.9.4"
  },
  "devDependencies": {
    "@ohm-js/cli": "^1.1.0",
    "@types/node": "^18.11.18"
  }
}
