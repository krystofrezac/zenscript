{
  "name": "zen-script",
  "version": "0.0.0",
  "author": "Kryštof Řezáč",
  "engines": {
    "node": "18.15.0"
  },
  "scripts": {
    "build": "turbo build",
    "dev": "pnpm build && turbo dev --parallel",
    "check:code": "turbo check:code"
  },
  "devDependencies": {
    "turbo": "^1.9.3"
  }
}