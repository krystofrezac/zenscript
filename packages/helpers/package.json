{
  "name": "@zen-script/helpers",
  "version": "0.0.0",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "lint": "eslint . --ext .js,.ts",
    "lint:fix": "npm run lint -- --fix",
    "check:type": "tsc --noEmit",
    "check:code": "pnpm check:type && pnpm lint"
  },
  "devDependencies": {
    "@zen-script/tsconfig": "workspace:*",
    "@zen-script/eslint-config": "workspace:*"
  }
}